# Update DNSv2

A simple guide on updating your DNS on windows.

import QueryParamCommand from '@site/src/components/QueryParamCommand.js';

press the Windows key + R on your keyboard to open the Run dialog box.

Type `ncpa.cpl` into the Run dialog box and press Enter.

Double click on your network adapter.

Click on properties.

uncheck IPv6

double click IPv4.

set DNS to manual and only set your primary dns.

save it.

## Validate DNS

Run the following commands in terminal/command prompt.

```terminal
ipconfig /renew
```

```terminal
ipconfig /flushdns
```

___


run this command with your local dns server IP:
<QueryParamCommand text="nslookup" domain="app.example.com" ip="10.0.0.123" queries={['domain', 'ip']} />

run this command without the dns ip:

<QueryParamCommand text="nslookup" domain="app.example.com" queries={['domain']} />